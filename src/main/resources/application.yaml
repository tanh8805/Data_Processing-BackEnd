spring:
  profiles:
    active: secret
  application:
    name: data_processing-be
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  datasource:
    url: jdbc:postgresql://${db.host}:5432/data_processing_be
    username: tuananh
    password: ${db.password}
    driver-class-name: org.postgresql.Driver

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}